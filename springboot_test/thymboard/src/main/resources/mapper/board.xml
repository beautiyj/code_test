<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.BoardDao">

	<insert id="insert" parameterType="board">
		INSERT INTO myboard
		VALUES
		(myboard_seq.NEXTVAL, #{writer}, #{passwd}, #{subject}, #{content}, 0,
		SYSDATE)
	</insert>

	<!-- 값을 돌려주는 타입 int -->
	<select id="count" resultType="int">
		SELECT COUNT(*) FROM myboard
	</select>


	<!-- 글 목록 받아오고 돌려주고 -->
	<select id="list" parameterType="int" resultType="board">
		SELECT * FROM (
		SELECT ROWNUM rnum, board.* FROM (
		SELECT * FROM myboard
		ORDER BY no
		DESC
		) board
		)
		WHERE rnum BETWEEN ((#{page} - 1) * 10 + 1) AND (#{page} *
		10)

	</select>

	<!-- 상세페이지 조회수 증가 -->
	<update id="updatecount" parameterType="int">
		UPDATE myboard SET
		readcount = readcount + 1 WHERE no = #{no}
	</update>

	<!-- 상세페이지 상세정보 구하기 -->
	<select id="content" parameterType="int" resultType="board">
		SELECT * FROM myboard WHERE no=#{no}
	</select>

	<!-- 글 수정 폼 -->
	<update id="update" parameterType="board">
		UPDATE myboard SET
		writer =
		#{writer}, subject=#{subject}, content=#{content},
		register=#{register},
		WHERE no = #{no}
	</update>


</mapper>



